# CI gating components-only fixture

This temporary document validates the components-only pull-request path tracked by issue #73.

Expected classification:

- root validation: skipped
- components validation: run
- native validation: skipped
- stable required checks: terminal and successful

This fixture must not be merged.
